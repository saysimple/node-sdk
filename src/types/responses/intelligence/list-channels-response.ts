/**
 * Channels is a list of channel objects containing a channel value and an identifier
 */
export interface ListChannelsResponseInterface {
    channel                 : string,
    tenantChannelIdentifier : string
}
